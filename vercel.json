{
  "buildCommand": "cd frontend && NODE_OPTIONS=--max-old-space-size=6144 npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
